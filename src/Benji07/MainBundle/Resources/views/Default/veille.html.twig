{% extends "::base.html.twig" %}

{% block body %}

    {% for link in pagination %}
        <h5><a href="{{ link.url }}">{{ link }}</a> <small>{{ link.createdAt|format_datetime }}</small></h5>
    {% endfor %}

    <div class="navigation">
        {{ pagination.render()|raw }}
    </div>
{% endblock %}